# 脚手架 · AI 说明

你在「业务知识库脚手架」目录。这里**不承载方法规则本身**——规则的唯一权威源是 `_新建知识库骨架/AGENTS.md`（自包含的操作规范）。本目录只是入口。

## 当用户说"跟我一起对 <产品> 的 <业务线> 做业务知识沉淀"
你来完成建库，不要让用户手动复制：
1. **复制** `_新建知识库骨架/` 整个文件夹 → 改名 `<业务线>-kb`，放到与本脚手架并列的位置。
2. **填占位符**：替换新目录里 `AGENTS.md`、`业务认知/00_总览.md`、`变更记录.md`、`索引.md` 中的 `<产品>` `<业务线>` `<模块…>` 等为实际名称。
3. **按业务模块建文件**：和用户确认模块划分后，为每个模块复制 `业务认知/_模块模板.md` → 改名 `<模块>.md`，并在 `索引.md` 登记。
4. 之后**在那个新 KB 目录里工作**，一切以它自己的 `AGENTS.md` 为准（读设计源、提取业务认知、记变更、更新索引）。

## 边界
- **不要在 `_新建知识库骨架/` 里直接提取知识或写业务内容**——它是待复制的模板，写脏会污染此后每个新库。永远先复制出去再干活。
- 不要把方法规则复述到本目录或 README——顶层只做入口。
- 面向人的说明见 `README.md`。
- 两个已建成实例可参考：`../hcp-3rdpartyintegration-kb-main`、`../hcp-parking-kb`。
