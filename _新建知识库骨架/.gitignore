# ─────────────────────────────────────────────
# 只把「知识核心」纳入版本库；派生物与大素材不上传，
# 这样别人只想读知识时无需拉取整个文件夹。
# ★ 首次提交前就放好本文件；若素材已被提交过，历史仍会变大，
#   需 `git rm -r --cached <路径>` 再提交（必要时用 BFG 清历史）。
# ─────────────────────────────────────────────

# 切片等中间产物（读图时临时生成，可随时重跑）
_临时/
_临时*/

# 按需产出：讲解 HTML 等，含内嵌图片、体积大、可从知识库重生
# 保留说明文件；生成的产物不入库（要分享就发布成 Artifact）
产出/*
!产出/README.md

# 设计源：原始画板 / 白皮书 / 环境截图 —— 体积最大的部分
# 知识已提取进 业务认知/，纯读知识库的人无需拉取
# 保留占位说明，忽略实际素材
设计源/*
!设计源/放这里.md
# ↑ 方案A（默认）：素材放共享盘/对象存储，仓库只留知识。
#   方案B：若要在仓库里可追溯地保存设计源，改用 Git LFS 跟踪
#   （例：git lfs track "设计源/**"），别人可 GIT_LFS_SKIP_SMUDGE=1 跳过下载。
#   两种方案都不要把大二进制直接提交进普通 git。

# OS / 编辑器杂物
.DS_Store
Thumbs.db
Desktop.ini
*.tmp
